[project]
name = "coyote_control"
version = "0.3.6"
description = "N.E.K.O plugin: let the catgirl control DG-Lab (coyote) devices via WebSocket V4 protocol"
requires-python = ">=3.10"
dependencies = [
    "websockets>=13.0",
    "aiohttp>=3.9.0",
]
